#include "main.h"
/**
 * _abs - calculate the absolute value of an integer
 * @r: the int in question
 * Return: the abs of the int.
 */

int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (-r);
}
