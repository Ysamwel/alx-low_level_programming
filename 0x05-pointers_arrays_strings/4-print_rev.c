#include "main.h"
/**
 * print_rev - print a string in reverse
 * @s: is the char
 */


void print_rev(char *s)
{
	int a, b;

	b = 0;
	while (s[b] != '\0')
		n++;

	for (a = b - 1; i >= 0; i--)
	{
		_putchar(s[i]);
	}
	_putchar('\n');
}
